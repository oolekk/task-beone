client {
  kafka {
    address = "localhost:9092"
    topic = "events"
    group = "group"
    client = "client"
  }
  rest {
     status-url = "http://localhost:8090/status"
     load-game-url = "http://localhost:8090/load"
     save-game-url = "http://localhost:8090/load"
  }
}